The contracts are already deployed and connected.
The server runs on node using expess.

Server start guide:
1. In terminal, change directory to /frontend
2. In terminal, enter "npm install"
3. In terminal, enter "npm serve"

The local server should be running on "http://localhost:3000"